document.addEventListener("DOMContentLoaded", () => {
  console.log("Dashboard loaded");

  // Example: refresh stats (dummy)
  function refreshStats() {
    console.log("Refreshing dashboard stats...");
  }

  refreshStats();
});
